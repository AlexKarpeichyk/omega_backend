apiVersion : apps/v1beta1
kind: Deployment
metadata:
  name: test 
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: test 
    spec:
      containers:
        - name: test 
          image: omegacontainerregister.azurecr.io/test
          ports:
          - containerPort: 5000